# NEXORA Pure CSS version

Tailwind CDN and `tailwind.config` runtime scripts were removed from all JSP pages. Static CSS is generated under `assets/css/pages/`. The original JSP DOM/content, responsive utility behavior, links, inline styles and JavaScript were retained. Google Fonts, Pretendard and Material Symbols stylesheet links remain because they provide fonts/icons rather than Tailwind layout styling.
